[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm config set legacy-peer-deps true", "npm install --legacy-peer-deps --no-audit --no-fund"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
